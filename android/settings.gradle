pluginManagement { def flutterSdkPath={def p=new Properties();file('local.properties').withInputStream{p.load(it)};assert p.getProperty('flutter.sdk')!=null:'flutter.sdk not set';p.getProperty('flutter.sdk')}();includeBuild("${flutterSdkPath}/packages/flutter_tools/gradle");repositories{google();mavenCentral();gradlePluginPortal()} }
plugins { id 'dev.flutter.flutter-plugin-loader' version '1.0.0'; id 'com.android.application' version '8.6.1' apply false; id 'org.jetbrains.kotlin.android' version '2.1.20' apply false }
dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS);repositories{google();mavenCentral()} }
rootProject.name='finaro';include ':app'
