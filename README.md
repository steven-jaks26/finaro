# Finaro Flutter — Phase 4

Offline-first Persian RTL Android foundation.

Requirements: Flutter stable/Dart >= 3.12, Android SDK, Java 17.

```bash
cp android/local.properties.example android/local.properties
flutter pub get
flutter analyze
flutter test
flutter build apk --release
```

See `PHASE4_UI_UX.md` and `PHASE4_COMPLETION.md`.
# finaro
